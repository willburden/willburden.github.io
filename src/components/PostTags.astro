---
import LinkCluster from "./LinkCluster.astro"

interface Props {
    tags: string[],
}

const { tags } = Astro.props;

const links = tags.map(tag => ({
    text: tag,
    href: `/tag/${tag}`
}));
---

<LinkCluster {links} />
